# Shared Data

Immutable base datasets used by training runs. Each subdirectory contains
generated or collected data that can be referenced by multiple runs.

Do not modify files here after creation — runs depend on them being stable.
